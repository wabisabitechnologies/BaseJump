<% if @todo.todo_list_id.present? %>
  <%= turbo_stream.append :todos, partial: "todos/todo", locals: { todo: @todo } %>
<% else %>
  <%= turbo_stream.append "loose-todos", partial: "todos/todo", locals: { todo: @todo } %>
<% end %>
